<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %} - Fabor</title>
        <link rel="stylesheet" href="{{ asset('bundles/app/css/bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/app/css/comp.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/app/css/dropzone.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/app/css/profile.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/app/css/MAgallery.min.css') }}">
        <link rel="stylesheet" href="{{ asset('bundles/app/css/main.css') }}">
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <!-- navbar -->
        <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
            <div class="container">
                <div class="navbar-header text-center">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" >
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href="{{ path('homepage') }}" >
                        <img src="{{ asset('bundles/app/img/Beats-logo.png') }}" alt="" class="navbar-brand">
                    </a>
                </div><!-- navbar-header -->

                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <div class="navbar-collapse collapse" id="navbar">
                        <!-- Work to be done , so messy -->
                        <div class="nav navbar-nav pull-right">
                            <ul class="notification-bar">
                                <li>
                                    <a href="#" class="holo">
                                        <i class="icon glyphicon glyphicon-bell"></i>
                                        <span class="badge">150</span>
                                    </a>
                                </li>
                                <li>
                                    <a href="#" class="holo">
                                        <i class="icon glyphicon glyphicon-transfer"></i>
                                        <span class="badge">7</span>
                                    </a>
                                </li>
                                <li>
                                    <a href="{{ path('app_profile_profile') }}" class="holo">
                                        <img class="img-circle box-size" src="{{ asset('bundles/app/img/avatar1.jpg') }}" alt="Logo"><span class="username">{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}</span>
                                        <a class="holo" href="{{ path('fos_user_security_logout') }}">
                                           <span class="glyphicon glyphicon-log-out"></span> {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                                        </a>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                {% else %}
                    <div class="pull-right">
                        <ul class="notification-bar">
                            <li>
                                <a class="holo" href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                            </li>
                            <li>
                                <a class="holo" href="{{ path('fos_user_registration_register') }}">Register</a>
                            </li>
                        </ul>

                    </div>

                {% endif %}

            </div>
        </nav>
        <main>
            {% if app.request.get('_route') == 'homepage' %}
                <div class="search-bar">
                    <div class="container">
                        <div class="row">
                            {{ form_start(form, {
                                'attr': {'class': 'form-inline'}
                            }) }}
                            <div class="col-lg-4">
                                <div class="form-group">
                                    {{ form_widget(form.item, {
                                        'attr': {'class':'form-control'}
                                    }) }}
                                </div>
                            </div>
                            <div class="col-lg-3">
                                <div class="form-group">
                                    {{ form_widget(form.category, {
                                        'attr': {'class':'form-control'}
                                    }) }}
                                </div>
                            </div>
                            <div class="col-lg-3">
                                <div class="form-group">
                                    {{ form_widget(form.city, {
                                        'attr': {'class':'form-control'}
                                    }) }}
                                </div>
                            </div>
                            <div class="col-lg-2">
                                <div class="form-group">
                                    {{ form_widget(form.submit, {
                                        'attr': {'class': 'btn btn-circle pull-right'}
                                    }) }}
                                </div>
                            </div>

                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            {% else %}

            {% endif %}

            <div class="container" style="margin-top: 50px">
            {% block body %}{% endblock %}
            </div>
        </main>
        <footer>
            <div class="container">
                <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusantium adipisci aliquam asperiores assumenda aut, dolorum eius eligendi enim eos error magnam nam nostrum quam ratione reiciendis sunt unde voluptate voluptatibus.</p>
            </div>
        </footer>
        <!-- Loading the scripts -->
        <script type="text/javascript" src="{{ asset('bundles/app/js/jquery-1.11.2.min.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/app/js/jquery-ui.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/app/js/bootstrap.min.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/app/js/MAgallery.min.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/app/js/dropzone.js') }}"></script>
        <script type="text/javascript" src="{{ asset('bundles/app/js/animation.js') }}"></script>

        {% block javascripts %}{% endblock %}

    </body>
</html>
